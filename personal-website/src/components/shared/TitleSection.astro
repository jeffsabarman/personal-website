---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div class="flex flex-col space-y-4">
  <p class="text-primary text-xl font-bold">{title}</p>
  <hr class="w-16 text-secondary border-2" />
</div>
